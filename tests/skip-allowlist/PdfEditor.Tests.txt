# Skips allow-listed for PdfEditor.Tests. See scripts/check-skip-budget.sh (#619).
# Every line is coverage we are NOT getting. Justify it or delete it.
# Format:  TestName   # why
#
PdfEditor.Tests.Integration.SearchPerformanceTests.Search_OnSyntheticDoc_CompletesInReasonableTime   # perf assertion; skips when the synthetic fixture is absent. Timing test, not correctness.
PdfEditor.Tests.UI.InPageLinkClickTests.ClickOnTocLink_NavigatesToDestinationPage   # #653: this test's real-book fixture path was a stale hardcoded personal path and had never actually run; now that it's fixed, it reveals a pre-existing (not #625-related) headless-layout timing issue against that specific 455-page book. Tracked, not yet root-caused.
PdfEditor.Tests.UI.MouseInputTests.ClickOnLinkAnnotation_FiresLinkClickedEvent   # #653: same pre-existing real-book layout-timing issue as InPageLinkClickTests above.
PdfEditor.Tests.UI.MouseInputTests.HoverOverLinkAnnotation_IndicatesInteractivity   # #653: same pre-existing real-book layout-timing issue.
PdfEditor.Tests.UI.MouseInputTests.MouseWheelScrollDown_ScrollsViewerVertically   # #653: same pre-existing real-book issue, but for scroll offset rather than link hit-testing — confirms the root cause is generic to this book/headless-mode, not link-specific.
PdfEditor.Tests.Unit.MainWindowViewModelTests.HiddenTextToggles_DoNotLoadOcrAssemblyBeforeRasterizedScan   # ORDER-DEPENDENT by nature: once any earlier test loads Pdfe.Ocr, "is it loaded?" cannot distinguish "we pulled it in" from "it was already here". Proving it needs a dedicated process. The weaker, order-INDEPENDENT half (watch AssemblyLoad during the toggle) now always runs. This skip is why the budget exists: on 2026-07-13 it silently stopped running and the suite stayed green.
